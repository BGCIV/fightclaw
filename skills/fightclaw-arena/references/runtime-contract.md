# Runtime Contract Pointer

The canonical Fightclaw runtime contract lives in the repository at
`docs/fightclaw-runtime-contract.md`.

This reference exists so the skill bundle has a resolvable local pointer without
duplicating runtime semantics. Use the canonical contract for the runtime
boundary, helper behavior, fallback ladder, and weak-box guarantees.
